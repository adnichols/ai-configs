# Prototype

Read the parent skill and [Codex runtime contract](../references/codex-runtime.md).

State the unresolved empirical question and observations that would distinguish the options. Build the cheapest isolated sketch in an explicitly named temporary location, preserving existing work. The driver creates and runs it; bounded read-only analysis may compare results. Measure behavior instead of asking the user for an observable fact. Return the result, limitations, and recommendation. A prototype is not a production implementation or permission to publish it.
