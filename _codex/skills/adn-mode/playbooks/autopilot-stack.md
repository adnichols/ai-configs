# Autopilot stack

Read the parent skill and [Codex runtime contract](../references/codex-runtime.md).

Plan the requested changes in dependency order and preserve the user's instruction about who lands the stack. The driver implements each complete unit and verifies it before building dependents. Use ordinary Git or an already configured stack tool according to repository policy; do not introduce Graphite as a dependency. Review against each unit's real base and keep candidate evidence current. Publish only if authorized. Never merge a stack the user reserved for their own landing.
