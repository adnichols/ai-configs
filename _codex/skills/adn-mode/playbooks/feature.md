# Feature

Read the parent skill and [Codex runtime contract](../references/codex-runtime.md).

Identify the requested behavior and acceptance criteria. Use `how` to trace the affected system. Name the data shape, ownership, and boundaries; use `architect` for a consequential unresolved design. Keep a short execution plan, note dependent steps and shared state, and implement in the driving session. Verify the actual behavior with appropriate tests and the relevant product control tools. Obtain bounded material review when required. Finish the authorized slice. Use opening-a-pr only when publication belongs to the task. Report behavior, choices, verification, and remaining limitations.
