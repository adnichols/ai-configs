# Hermes Daily Pi Analytics Morning Review — Implementation Review

**Method:** Read the plan contract; diffed `origin/main` vs. working tree for all touched ai-configs files; read supplied mbp Good Morning snapshots (`orchestrator.py`, `pi_analytics_phase.py`, `publish.py`, `render.py`, `test_gm_pi_analytics_publish.py`, `range.diff`); ran the local test suite; ran `refresh-manifest`/`verify`/component `install --dry-run` myself; hand-traced the P1 analyzer fixture math line-by-line against the asserted counts; cross-checked prior Codex plan-review findings against current code to confirm they're actually fixed, not just claimed fixed.

## Failure family 1 — Privacy boundary & completed-day determinism (`scripts/pi_session_analytics.py`)

- **IN_PLAN** — `extract_allowed_record` (`scripts/pi_session_analytics.py:110-130`) only ever reads the allowlisted keys; `content`, `prompt`, `thinking`, `credentials`, `toolCall`, `source`, `path` are never touched. Verified by manually tracing `scripts/tests/fixtures/pi-session-analytics/comprehensive/session.jsonl` against `test_comprehensive_fixture_locks_v1_aggregate` (`scripts/tests/test_pi_session_analytics.py:40-77`) — every count (`lines_total=11`, `message_records_in_window=10`, `assistant_terminal_attempts=8`, `error_attempts=7`, one of each category) reproduces exactly by hand.
- **IN_PLAN** — Out-of-window append invariance confirmed both by code (`malformed_lines` reserved at `0`; window filter at `pi_session_analytics.py:199-200`) and by `test_out_of_window_appends_and_files_do_not_change_daily_report_identity` (`scripts/tests/test_pi_session_analytics.py:158-180`), which passes.
- **IN_PLAN** — Symlink escape prevention (`input_files`, `pi_session_analytics.py:150-163`, `resolved_path.is_relative_to(resolved_root)`) confirmed by `test_symlinked_jsonl_cannot_escape_explicit_root` (`scripts/tests/test_pi_session_analytics.py:182-198`), passing.
- **IN_PLAN** — Classification only reads `errorMessage`, never `content` — confirmed by `test_only_error_message_drives_classification` (`scripts/tests/test_pi_session_analytics.py:119-138`), passing.
- No P1/P2 risk found in this family.

## Failure family 2 — Collector locking, C-Core publication, additive component deployment

- **IN_PLAN** — Same-host serialization via `fcntl.flock` in `collector_lock` (`_hermes/default/scripts/pi_analytics_collector.py:401-412`); `test_collector_lock_serializes_overlapping_host_runs` (`scripts/tests/test_pi_analytics_deploy.py:112-141`) verifies a second concurrent run blocks until the first releases. This is the previously-flagged "overlapping collector creates" risk — fixed and tested.
- **IN_PLAN** — `reconcile_existing`/`publish_report` (`pi_analytics_collector.py:338-398`) correctly distinguishes reuse-identical (no mutation), `ReportConflict` (differing content, same title — the one path that can legitimately produce `report_conflict`, matching the disclosed 2026-07-19 rollout fact), and safe retry-after-interrupted-create. Confirmed by `test_failed_create_reconciles_interrupted_run` and `test_conflicting_same_title_report_is_rejected` (`scripts/tests/test_pi_analytics_ccore_publish.py`), both passing. Nothing in the collector or docs claims a conflicting historical report was "regenerated" — `report_conflict` is reported truthfully, not silently overwritten.
- **IN_PLAN** — Additive component merge (`merge_component_job`, `scripts/hermes_config_sync.py:598-621`) preserves unrelated jobs and host-set runtime fields, verified end-to-end by `test_component_install_is_additive_and_preserves_unrelated_jobs_and_runtime` (`scripts/tests/test_hermes_config_sync_component.py:134-179`), which I confirmed passes. I independently ran `--component pi-analytics-collector install --dry-run` against a scratch `--hermes-home`: output listed exactly `scripts/pi_analytics_collector.py` and job `a91c0d7e4b22`, nothing else.
- **IN_PLAN** — Stale full-manifest rejection before any component operation (`validate_component_source`, `scripts/hermes_config_sync.py:686-706`) confirmed by `test_component_operations_reject_stale_full_manifest`, passing. I ran `refresh-manifest` + `verify` myself against the actual bundle: `REFRESHED MANIFEST: 1701 source files` / `VERIFY OK: 1701 files, no obvious secret patterns` — matches the claimed evidence.
- No P1/P2 risk found in this family.

## Failure family 3 — Restricted routed actions, idempotent ledger, generic-worker fallthrough prevention

- **IN_PLAN** — `dispatch_claim` (`_hermes/default/scripts/gm_plan_comment_listener.py:248-257`) routes every `pi-analytics-card-` prefixed anchor — including malformed ones — to the restricted worker before the generic `start_worker` path can see it. Confirmed by `test_every_analytics_prefixed_claim_is_restricted_before_generic_worker` (`scripts/tests/test_gm_pi_analytics_action.py:300-320`), covering both well-formed and malformed card anchors, both `agent` and `comment` submit actions.
- **IN_PLAN** — Registry-workspace binding before any Doct mutation. This is the specific bug the prior `run-plan-pm-codex.md` review caught ("A rejected claim with a mismatched workspace still mutates that untrusted workspace"). Current `reject_claim` (`_hermes/default/scripts/pi_analytics_action.py:405-435`) looks up the registry entry for the claimed `document_id` and returns silently if `entry.get("workspace_id") != workspace_id`, before issuing any `reply`/`ack`/`resolve`. Confirmed fixed by the "wrong registry workspace" subtest in `test_wrong_author_malformed_stale_and_identity_mismatches_are_rejected` (`scripts/tests/test_gm_pi_analytics_action.py:192-213`), which now asserts zero plan actions for that case — matches the `run-plan-pm-codex-postfix.md` "Resolved" note.
- **IN_PLAN** — Full identity chain (submitAction=agent, card-anchor regex, author user ID, document/workspace/thread cross-field consistency, document version, generated-HTML-hash/source-hash agreement, registry current-entry match, signal/evidence match) enforced in `validate_claim` (`pi_analytics_action.py:159-260`); `require_current_document_version` re-checks the live document version via `doct-agent documents get` before recording a decision (`pi_analytics_action.py:367-383`), closing the TOCTOU window between claim and processing.
- **IN_PLAN** — Idempotent ledger: `delivery_key` = SHA-256 over document/version/card/evidence (`pi_analytics_action.py:305-312`); duplicate/crash-retry produces exactly one delivery and does not extend the display window — confirmed by `test_duplicate_and_crash_retry_record_one_decision_without_extending_window` (`scripts/tests/test_gm_pi_analytics_action.py:216-250`).
- **IN_PLAN** — Restricted worker uses fixed Python argv (`sys.executable`, `pi_analytics_action.py`, `--claim-file`) with no `hermes chat`, no shell, `start_new_session=True` — confirmed by `test_restricted_worker_uses_fixed_python_argv_without_hermes_or_shell` and `test_restricted_commands_cannot_reach_external_mutation_systems`, both passing; only `reply`/`ack`/`resolve`/`release` Doct calls are ever issued.
- **IN_PLAN** — Host-ownership separation: `pi_analytics_action.py` (the restricted worker) is in the full bundle manifest but explicitly excluded from the `pi-analytics-collector` component (`test_restricted_worker_is_in_full_bundle_but_not_collector_component`, `scripts/tests/test_gm_pi_analytics_action.py:269-278`), so a dever component install cannot gain card-action authority even indirectly.
- No P1/P2 risk found in this family.

## Failure family 4 — Remote Good Morning reader (mbp snapshot: `pi_analytics_phase.py`, `render.py`, `publish.py`, `orchestrator.py`)

- **IN_PLAN** — `ccore_space` fail-closed. This is the specific bug the `run-plan-pm-codex-postfix.md` review caught ("An explicit empty or non-string `ccore_space` is silently replaced with the Private default, then invokes C-Core"). The snapshot I was given (`pi_analytics_phase.py:158-172`, same timestamp as the postfix review) now raises `AnalyticsDataError` whenever `"ccore_space" in payload and payload["ccore_space"] != "Private"` — this covers `""`, `None`, `7`, and any other non-`"Private"` value, and the raise happens before any `ccore` subprocess call. `test_reader_rejects_every_explicit_non_private_space_without_invoking_ccore` (`test_gm_pi_analytics_publish.py:118-131`) exercises exactly `("Other", "", None, 7)` with a runner that asserts it's never called, and asserts the result degrades to the `incomplete` warning. This matches the task's "already-fixed" claim — I confirm it is in fact fixed in the supplied snapshot, not merely asserted.
- **IN_PLAN** — Two-host freshness/duplicate/schema handling in `_host_report` (`pi_analytics_phase.py:189-237`): exactly-one-match→fresh, older-only→stale, any future or >1 match→invalid, none→unavailable. Covered by `test_missing_stale_future_duplicate_invalid_and_unreadable_suppress_all_cards`.
- **IN_PLAN** — Display disposition semantics for all four D4 actions traced against `_apply_dispositions` (`pi_analytics_phase.py:306-371`): accept/investigate show latest evidence for 7 days via `visible_until` while also suppressing the normal card; defer suppresses via `suppress_until` regardless of changed counts; dismiss stays suppressed only while `crossing and dismissed_active`, and a single below-threshold report clears the disposition (new crossings become a new, unsuppressed card) — matches D4/BDD-8 exactly and is covered by `test_dismiss_stays_hidden_across_counts_resets_below_threshold_and_later_recurs`.
- **IN_PLAN** — Final registry parity: `publish.py:391-421` extracts `card_id`/`signal_key`/`evidence_snapshot_id` from the actually-rendered HTML (not from the phase payload), and `pi_analytics_action.py` reads the identical field names (`document_version`, `html_sha256`, `pi_analytics_cards`) from the registry entry `publish.py` writes — the cross-repo contract lines up field-for-field.
- **IN_PLAN** — Nonfatal orchestration: `pi_analytics_phase.collect()` wraps its entire body in a broad `except Exception` that degrades to the incomplete-warning payload without leaking exception text (`pi_analytics_phase.py:531-534`), and even if a phase somehow still raised, `orchestrator.run_phase` (`orchestrator.py:32-42`) isolates it to a per-phase error without blocking HTML render/publish. `render._pi_analytics_section` degrades gracefully on an `{"ok": False}` payload (no matching `status`, falls through to the generic incomplete-host block) rather than crashing the renderer.
- No P1/P2 risk found in this family (reviewed via code + the supplied snapshot's own test file; I could not execute this suite myself since it lives in a separate repository not present here — see Incomplete note below).

## QUESTION (non-blocking, evidence-accuracy only)

- The earlier verification summary contained a stale test count. I ran the actual suite myself: `test_pi_session_analytics.py` (12) + `test_pi_analytics_ccore_publish.py` (6) + `test_pi_analytics_deploy.py` (9) + `test_hermes_config_sync_component.py` (8) = **35 tests**, all passing (`Ran 35 tests ... OK`). The restricted-action suite added 12 more passing tests, for a verified total of **47 tests**. The discrepancy was an evidence-counting error, not missing coverage.

## Completed vs. remaining checks

**Completed:** P1 analyzer contract (schema, categories, privacy, symlink/append invariance) — full read + fixture hand-trace + test run. P2 collector (locking, publish/reconcile, status states, component manifest/install/verify, additive merge, stale-manifest rejection) — full read + test run + my own `refresh-manifest`/`verify`/`install --dry-run` execution. P4 restricted action worker and listener dispatch — full read + test run, including tracing the two specific bugs the prior Codex rounds found (workspace-mutation-on-reject, `ccore_space` fail-open) through to their fixes. P3 remote reader/renderer/publisher/orchestrator — full read of the supplied snapshot and its test file, with manual trace of freshness/disposition/registry logic; **not independently executed** (lives in a separate repo not available in this sandbox), consistent with the plan's own PM review noting the same limitation.

**Remaining:** None within the stated scope contract. Cross-host convergence (mbp reading a disposable dever-published aggregate) and the live dever component `install --apply`/`verify` against a real `~/.hermes` are runtime/deployment steps outside what a static repo review can execute; the plan's own verification section treats these as rollout-time gates, and the code paths that implement them (`reconcile_existing`, `install_component`, `verify_component`) are exercised by passing unit tests.

VERDICT: PASS_SCOPED
