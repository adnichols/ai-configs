- **P1 “Tests first” / P4 “Tests first” and AC6–AC7 — model retirement is not safely specified.** The planned fixture preserves only an unrelated provider, but the current pruning deletes *any* absent `openai-codex` model whose display name ends in `(CLI Proxy API)` ([install.sh](/Users/anichols/code/ai-configs/install.sh:1914)). That violates AC7 for caller-owned models in that provider. Also, `enabledModels` removes Spark/Grok only, not GPT-5.4 ([install.sh](/Users/anichols/code/ai-configs/install.sh:1741)), leaving stale GPT-5.4 registrations after the source-model deletion. Smallest correction: define retirement by the exact two managed IDs, prune their `enabledModels` forms, and test both removal plus preservation of a custom `openai-codex` CLI Proxy model.

- **P3 “Work” / BDD6 — the direct worktree replacement lacks the current command contract.** “Collision handling” does not specify the existing branch/path scheme, `.ltui.json` project check, same-branch collision behavior, Linear context note, or success/error output. Those are currently implemented by the deleted agent ([worktree-creator.md](/Users/anichols/code/ai-configs/_pi/agents/worktree-creator.md:44)). The force-removal behavior for an existing same-branch worktree is particularly material. Smallest correction: add this source-derived behavior map and targeted command assertions; explicitly retain the force-recreate behavior or record a deliberate safe-stop behavior change.

- **P4 “Install parity” — the stated host verification cannot prove AC1/AC6.** The normal verifier does not inspect installed agents or retired models; its review-stack mode only checks each expected source entry and permits extras ([verify-pi-install.sh](/Users/anichols/code/ai-configs/scripts/verify-pi-install.sh:34)). Moreover, the existing transaction test exercises `--pi-review-stack` ([test_install_pi_transaction.py](/Users/anichols/code/ai-configs/scripts/tests/test_install_pi_transaction.py:38)), whose copy loop leaves retired agent files in place ([install.sh](/Users/anichols/code/ai-configs/install.sh:2228)). Smallest correction: make the isolated exact-roster test exercise the production `--pi` path (or explicitly change review-stack pruning), and add exact agent-set plus retired-model/settings absence checks to the post-install verifier.

VERDICT: PLAN_NEEDS_REVISION