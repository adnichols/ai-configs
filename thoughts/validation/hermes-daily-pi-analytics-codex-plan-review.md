Completed slices: 1–4. No remaining slice.

- `READINESS_BLOCKER` — P2’s collector-profile install command cannot run as written. It passes `_hermes/default/profiles/pi-analytics` as a bundle, but the sync tool requires a bundle-root `manifest.json`; profile directories are installed only when nested under the full `_hermes/default` bundle. The plan also omits the scheduler/launcher contract that makes a separate profile home execute its 05:00 cron. Evidence: [plan P2](../plans/hermes-daily-pi-analytics-morning-review.html#L60); [sync tool](../../scripts/hermes_config_sync.py#L525).

- `READINESS_BLOCKER` — The C-Core retention contract is not implementable for the proposed “document” reports. The plan requires deletion after 45 days, but repo guidance says `ccore doc` provides list/new/show, not update/delete; only typed managed objects have supported deletion. Select a supported report object/lifecycle and its exact create/read/idempotency/delete operations, or remove deletion from v1. Evidence: [plan D5/P2](../plans/hermes-daily-pi-analytics-morning-review.html#L30); [C-Core caveat](../../_hermes/default/profiles/nerd/skills/software-development/ccore-cli-operations/SKILL.md#L261).

- `READINESS_BLOCKER` — P4 does not define a safe, working comment ingress. Existing Good Morning listener processes only routed Doct plan work; ordinary comments are explicitly excluded, while its current handler dispatches a broadly capable Hermes worker. The plan says Aaron can “comment” on a card and promises a restricted action worker, but does not require routed action comments or specify the dispatcher change that prevents those comments reaching the generic worker. Evidence: [existing listener](../../_hermes/default/scripts/gm_plan_comment_listener.py#L1), [P4](../plans/hermes-daily-pi-analytics-morning-review.html#L64).

- `PRODUCT_QUESTION` — “Dismiss this exact evidence snapshot” versus “materially changed signal” is not deterministic. Define the card/snapshot identity inputs, the material-change rule, and whether accept/investigate/defer follow a host/category across changed daily counts. Without this, stable IDs, the SQLite idempotency key, and all four action tests cannot be implemented consistently. Evidence: [D4](../plans/hermes-daily-pi-analytics-morning-review.html#L29), [P3/P4](../plans/hermes-daily-pi-analytics-morning-review.html#L62).

- `OPTIONAL_CLARITY` — P1 should name the exact allowed JSON paths and exact mapping rules for structured error messages. “Such as … error code/message” leaves the analyzer’s input boundary open, even though the aggregate-only output and sentinel tests are otherwise well scoped. Evidence: [P1](../plans/hermes-daily-pi-analytics-morning-review.html#L56).

VERDICT: PLAN_NEEDS_REVISION