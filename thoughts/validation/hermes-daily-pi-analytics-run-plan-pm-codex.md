Findings:

1. **IN_PLAN** — A rejected claim with a mismatched workspace still mutates that untrusted workspace. `validate_claim` rejects it after registry comparison, but `process_claim` calls `reject_claim`, which issues Doct `reply`, `ack`, and `resolve` using the claim’s workspace ID ([pi_analytics_action.py](/home/anichols/code/ai-configs/_hermes/default/scripts/pi_analytics_action.py:217), [pi_analytics_action.py](/home/anichols/code/ai-configs/_hermes/default/scripts/pi_analytics_action.py:452), [pi_analytics_action.py](/home/anichols/code/ai-configs/_hermes/default/scripts/pi_analytics_action.py:405)). The test explicitly expects those mutations for `workspace-other` ([test_gm_pi_analytics_action.py](/home/anichols/code/ai-configs/scripts/tests/test_gm_pi_analytics_action.py:192)). This leaves prior finding 1d unresolved: no Doct mutation may occur before workspace equality is established.

2. **IN_PLAN** — The remote Good Morning reader does not enforce the required `Private` C-Core space. It accepts any configured nonempty `ccore_space` and passes it directly to `ccore doc list/show` ([pi_analytics_phase.py](/tmp/hermes-daily-pi-analytics-mbp-review/pi_analytics_phase.py:158), [pi_analytics_phase.py](/tmp/hermes-daily-pi-analytics-mbp-review/pi_analytics_phase.py:503)). The producer correctly rejects anything other than `Private` ([pi_analytics_collector.py](/home/anichols/code/ai-configs/_hermes/default/scripts/pi_analytics_collector.py:169)), but the reader needs the same fail-closed boundary. Existing remote tests only exercise `Private` ([test_gm_pi_analytics_publish.py](/tmp/hermes-daily-pi-analytics-mbp-review/test_gm_pi_analytics_publish.py:96)).

Completed checks: prior 1a–1c are fixed and directly covered—completed-day append invariance ([test_pi_session_analytics.py](/home/anichols/code/ai-configs/scripts/tests/test_pi_session_analytics.py:158)), resolved symlink containment ([test_pi_session_analytics.py](/home/anichols/code/ai-configs/scripts/tests/test_pi_session_analytics.py:182)), and host-local publication serialization ([pi_analytics_collector.py](/home/anichols/code/ai-configs/_hermes/default/scripts/pi_analytics_collector.py:401), [test_pi_analytics_deploy.py](/home/anichols/code/ai-configs/scripts/tests/test_pi_analytics_deploy.py:112)). The supplied Good Morning snapshots correctly implement two-host freshness/duplicate/schema suppression, deterministic capped cards, disposition behavior, final registry version/hash metadata, and nonfatal analytics warnings.

The known July 19 immutable-report conflict does not block correct behavior for subsequent completed days. It needs disclosure only if release documentation claims that July 19 was successfully regenerated/backfilled.

I could not independently rerun the local suite: this read-only sandbox cannot create temporary directories. The supplied 39/39 and remote 19/19 results remain the available execution evidence.

VERDICT: FIX_IN_SCOPE_FINDINGS