- P2 — Scope: component install backup isolation. Trigger: update an existing collector using `--component pi-analytics-collector --hermes-home <non-default> install --apply`. Path: `backup_path()` always roots backups at `DEFAULT_HERMES_HOME`, not the CLI-resolved `hermes_home`. Impact: the component overwrites files/jobs while placing their backups under another Hermes home (or raises for a sibling path sharing the default-home string prefix), breaking recovery and isolation. Diff: newly reachable through the component installer. Evidence: [main](../../../scripts/hermes_config_sync.py#L819) resolves the override but [install_component](../../../scripts/hermes_config_sync.py#L746) and [install_component_jobs](../../../scripts/hermes_config_sync.py#L723) call [backup_path](../../../scripts/hermes_config_sync.py#L421), which unconditionally uses `DEFAULT_HERMES_HOME`; the component test masks this by replacing that global before installation. Fix: pass the resolved `hermes_home` as the backup root through the component install helpers and add a non-default-home backup-location test. Blocking: yes.

VERDICT: FINDINGS_TO_RESOLVE