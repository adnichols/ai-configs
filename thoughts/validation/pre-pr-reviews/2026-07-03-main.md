# Pre-PR Implementation Review — main

Date: 2026-07-03

## Scope

- Base/range: `origin/main`
- Plan path: none supplied; standalone diff review
- Branch: `main`
- Review scope: committed changes against `origin/main`, staged changes, unstaged working-tree changes, and review artifact sanity when present.

## Review packet

Initial packet unexpectedly showed no diff. A follow-up status check showed unstaged changes, so the empty-diff review was treated as invalid for gate purposes and the packet was rebuilt from actual worktree state.

Corrected packet before fixes:

```text
## main...origin/main
 M _hermes/default/skills/software-development/run-plan/SKILL.md
 M _pi/agents/developer-glm.md
 M _pi/agents/general-glm.md
 M _pi/agents/quality-reviewer-glm.md
 M _pi/models.json
 M install.sh
 M scripts/verify-pi-install.sh
 M skills/plan-reviewer-execution-ready/SKILL.md
 M skills/pre-pr-implementation-review/SKILL.md
 M skills/run-plan/SKILL.md

--- committed diff stat ---
(empty)

--- staged diff stat ---
(empty)

--- unstaged diff stat ---
10 files changed, 23 insertions(+), 26 deletions(-)
```

Changed files reviewed in cycle 1:

- `_hermes/default/skills/software-development/run-plan/SKILL.md`
- `_pi/agents/developer-glm.md`
- `_pi/agents/general-glm.md`
- `_pi/agents/quality-reviewer-glm.md`
- `_pi/models.json`
- `install.sh`
- `scripts/verify-pi-install.sh`
- `skills/plan-reviewer-execution-ready/SKILL.md`
- `skills/pre-pr-implementation-review/SKILL.md`
- `skills/run-plan/SKILL.md`

Current changed files after fixes:

- `AGENTS.md`
- `scripts/verify-pi-install.sh`

## Cycle 1 reviewer verdicts

### GPT reviewer

- Verdict: `FINDINGS_TO_RESOLVE`
- Blocking findings:
  - P2 `REGRESSION_FROM_THIS_DIFF`: `scripts/verify-pi-install.sh` used bare substring `grep -Fq` checks for model routes, which could falsely pass on `pi --list-models` output such as `No models matching "...glm-5.2"`.

### GLM reviewer

- Verdict: `FINDINGS_TO_RESOLVE`
- Blocking findings:
  - P2 `REGRESSION_FROM_THIS_DIFF`: same route-verification false positive in `scripts/verify-pi-install.sh`.
  - P3 `REGRESSION_FROM_THIS_DIFF`: `AGENTS.md` still documented the migrated Pi GLM subagents as `opencode-zen/glm-5.2`.
- Non-blocking follow-ups:
  - `_omp/agents/developer-glm.md` still uses `opencode-zen/glm-5.2`, but that is OMP-side and outside this Pi migration. Tracking destination: separate OMP-side migration if/when OMP moves providers.
  - `_hermes/default/skills/software-development/run-plan/SKILL.md` and `skills/run-plan/SKILL.md` have a pre-existing Codex-instruction divergence outside this diff. Tracking destination: dedicated mirrored-surface parity pass if exact parity is intended.

## Triage table

| Finding | Reviewer | Severity | Scope | Decision | Evidence |
|---|---|---:|---|---|---|
| Model-route verifier false-positive on `No models matching` output | GPT, GLM | P2 | REGRESSION_FROM_THIS_DIFF | Fixed | Replaced bare substring grep with anchored provider+model table-row regexes and verified positive/negative cases against `pi --list-models`. |
| Stale Pi GLM catalog entries in `AGENTS.md` | GLM | P3 | REGRESSION_FROM_THIS_DIFF | Fixed | Updated `general-glm`, `developer-glm`, and `quality-reviewer-glm` catalog entries to `opencode/glm-5.2`; stale touched-surface grep is clean. |
| OMP-side `opencode-zen/glm-5.2` route | GLM | P3 | OUT_OF_SCOPE_FOLLOW_UP | Deferred | OMP installs separately from Pi and was not touched by this Pi route migration. Tracking destination: future OMP provider migration if needed. |
| Pre-existing Hermes-vs-skills run-plan Codex instruction divergence | GLM | P3 | OUT_OF_SCOPE_FOLLOW_UP | Deferred | Both touched files agree on `opencode/glm-5.2`; remaining instruction divergence predates this diff. Tracking destination: future mirrored-surface parity pass. |

## Fixes applied

- `scripts/verify-pi-install.sh`: changed reviewer route verification from bare substring matching to anchored provider+model row matching:
  - `openai-codex` + `gpt-5.5`
  - `opencode` + `glm-5.2`
- `AGENTS.md`: updated the three Pi GLM catalog entries to `opencode/glm-5.2`.

## Verification after fixes

- `bash -n scripts/verify-pi-install.sh` — PASS.
- Positive GPT route regex against real `pi --list-models 'openai-codex/gpt-5.5'` output — PASS.
- Positive GLM route regex against real `pi --list-models 'opencode/glm-5.2'` output — PASS.
- Negative fake GLM route regex against `pi --list-models 'not-a-provider/glm-5.2'` output — EXPECTED_FAIL.
- Stale touched-surface route search for `opencode-zen/glm-5.2` across `AGENTS.md`, `_pi/agents`, relevant skills, and Hermes run-plan skill — no output.
- Final changed-file check before artifact update showed only `AGENTS.md` and `scripts/verify-pi-install.sh` modified.

## Cycle 2 narrow rereview verdicts

### GPT reviewer

- Verdict: `CLEAN_FOR_PR`
- Scope checked: two-file diff (`AGENTS.md`, `scripts/verify-pi-install.sh`) and the two prior blocking findings.
- Findings: none.

### GLM reviewer

- Verdict: `CLEAN_FOR_PR`
- Scope checked: two-file diff (`AGENTS.md`, `scripts/verify-pi-install.sh`), prior blocking findings, positive and fake-provider route behavior, and stale touched-surface route references.
- Findings: none.

## Remaining follow-ups

Non-blocking out-of-scope follow-ups only:

1. `_omp/agents/developer-glm.md` remains on `opencode-zen/glm-5.2`; this is OMP-side, not Pi-side. Tracking destination: future OMP provider migration if needed.
2. Hermes run-plan and shared `skills/run-plan` still have a pre-existing Codex-instruction divergence. Tracking destination: future mirrored-surface parity pass if exact parity is intended.

## Final gate result

- GPT verdict: `CLEAN_FOR_PR`
- GLM verdict: `CLEAN_FOR_PR`
- Unresolved in-scope P1/P2 findings: none
- Fixed blocking P3 findings: stale `AGENTS.md` Pi GLM catalog entries
- Final status: `OPEN_PR_READY`
