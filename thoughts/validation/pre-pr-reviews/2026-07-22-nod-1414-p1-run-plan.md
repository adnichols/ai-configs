# NOD-1414 P1 run-plan review ledger

- Phase: P1 — Parallelize independent Claude and Codex review legs
- Base at phase start: `4e40025b41fbbc14d491086296ca3fadb0fe94b3`
- Branch: `nod-1414-parallel-review`
- Plan: Heddle `thoughts/plans/heddle-claude-codex-review-performance.html`
- Doct document: `c33d8618-4310-4db6-823c-dbf23980e9ff`
- Linear issue: `NOD-1414`

## Scope baseline

P1 installs and documents a runnable shared orchestration helper that captures one complete candidate fingerprint, submits all applicable reviewer prompts before the first wait, settles legs independently/concurrently, validates nonce/verdict/fingerprint per leg, aggregates fail-closed, permits one narrowed unusable-output retry, records deterministic timing evidence, and closes only coordinator-owned tabs after a clean durable artifact.

Out of scope: P2 packet classification, P3 slice reuse/cache, P4 durable artifact authentication, Herdr durable jobs, Heddle runtime behavior, private tmux, and unrelated ai-configs baseline-test repairs.

## Changed surfaces

- `scripts/review_orchestration.py`
- `scripts/benchmark_review_orchestration.py`
- `tests/test_review_orchestration.py`
- `tests/fixtures/heddle-dual-review/*`
- `install.sh`
- `scripts/install-pi-transactionally.sh`
- `scripts/verify-pi-install.sh`
- `scripts/tests/test_install_pi_transaction.py`
- `test_install_shared_skills.sh`
- `skills/autoreview/SKILL.md`
- `skills/herdr-reviewers/SKILL.md`
- `skills/run-plan/SKILL.md`
- `_hermes/default/skills/software-development/run-plan/SKILL.md`
- `_pi/prompts/dev:run.md`
- `_codex/prompts/dev:run.md`
- `_pi/README.md`
- `AGENTS.md`
- `skills/repo-agents-bootstrap/SKILL.md`
- `skills/repo-agents-bootstrap/references/root_agents_template.md`

## Review-policy correction requested during execution

The user rejected the former rule that pre-PR review must stop after three cycles while the adversarial consultation route was available only after PR feedback. The ai-configs policy now keeps the ordinary three-cycle budget but requires one read-only external consultation per stable, distinct `REVIEW_ESCAPE` failure-family/scope identifier, before or after PR creation. Verified reject/reclassify evidence can clear a family; only explicit authorization starts one bounded adversarial pass plus one pass after fixes; separate later families may receive their own consultation; the same identifier may not be renamed or reconsulted. External consultation is advisory and cannot become implementation authority.

The policy change received one implementation review, one targeted rereview, a final bounded fix, and a final `PASS_SCOPED` review.

## Verification evidence

Latest evidence after all P1 runtime, PM-completion, and pre-PR fixes:

- `python3 -m unittest tests.test_review_orchestration` — PASS, 73 tests.
- `python3 -m unittest scripts.tests.test_install_pi_transaction` — PASS, 16 tests.
- Combined orchestration and transaction suites — 89 tests PASS.
- Applicable complete Python test surfaces — 260 passed, 3 skipped after rebase onto current `origin/main`, across root, scripts, Hermes, and maintained review-skill suites.
- `python3 -m py_compile scripts/review_orchestration.py scripts/benchmark_review_orchestration.py tests/test_review_orchestration.py scripts/tests/test_install_pi_transaction.py` — PASS.
- `bash -n install.sh scripts/install-pi-transactionally.sh scripts/verify-pi-install.sh test_install_shared_skills.sh` — PASS.
- Five-sample serial benchmark after one discarded warmup — median `0.194042s`, p75 `0.195179s`, p90 `0.196472s`, accepted/applicable `10/10`, coverage `1.000`, cleanup complete `5/5`.
- Five-sample parallel benchmark after one discarded warmup — median `0.107436s`, p75 `0.108576s`, p90 `0.109924s`, accepted/applicable `10/10`, coverage `1.000`, cleanup complete `5/5`.
- Comparison — `44.633%` median improvement, threshold `>=35%`, coverage `1.000`.
- Benchmark schema v2 reports startup as `not_applicable` because the deterministic fixture uses pre-created in-memory reviewers and no tabs.
- Raw benchmark JSON remained outside the product diff under `artifacts/review-perf/` for hashing and was then removed: serial SHA-256 `18bcf893cf5352b7465a4373eea4661ef314101543f26eee26aa21fd6036d9a1`; parallel SHA-256 `bb5310f89dc9d7424aa2976da4dbd0df3acf1f080bf4e591d0431e1ced80ba68`; fixture SHA-256 `d7a0bc728f41d3906dbc3187349eb1e2c1524aef719e1cad424fef6def854ce3`.
- Benchmark host/tool record: `Aarons-MacBook-Pro.local`, `macOS-26.5.2-arm64-arm-64bit`, Python `3.9.6`, Git `2.50.1 (Apple Git-155)`, Herdr `0.7.5-preview.2026-07-21-0f10e1453a7f`; reviewer profiles Codex `gpt-5.6-terra/high/read-only/no-approval` and Claude `claude-sonnet-5/xhigh/Read,Grep,Glob`.
- Focused bounded installer, transactional rollback, runtime parity, and review-guidance tests — PASS.
- `git diff --check` — PASS.
- `bash test_install_shared_skills.sh` after rebase — candidate 19 passed / 8 failed versus current-main 18 passed / 8 failed. The candidate's new parallel-protocol install/source-parity test passes; the same eight baseline failures remain: skills-mode additive/idempotent behavior, two update-modifier cases, default-mode shared-sync reuse, extension preservation, Codex/Pi parity, stale complete-slice wording in the review-guidance test, and active-agent Kimi inventory.

## Initial three review cycles

| Cycle | Finding | Source | Decision |
|---|---|---|---|
| 1 | Generic secret-name filtering omitted legitimate source paths | Codex + Claude | Fixed with narrow secret conventions and inverse source-name coverage |
| 1 | Cleanup receipt/request/tab identity insufficiently bound | Codex + Claude | Fixed with canonical request identity and per-leg clean-verdict requirements |
| 1 | Partial prompt rejection still began sibling waits | Codex | Fixed with zero-wait batch abort |
| 1 | Benchmark did not admit semantic fixture evidence | Codex | Fixed with versioned fixture validation |
| 2 | Delimiter-aware filtering still omitted realistic source identifiers | Codex + Claude | Fixed across tracked/staged/unstaged/untracked fingerprint slices |
| 2 | Old receipt could close a reused active tab | Codex | Fixed with accepted transcript digest and all-leg liveness preflight |
| 3 | Optional `workspace_id` skipped recorded-tab membership validation | Codex | Sent to external consultation after policy correction |
| 3 | Manual receipt digest rewriting lacks cryptographic authentication | Codex | Classified as P4 durable-artifact authentication, not P1 valid supported input |
| 3 | Benchmark fake adapter cleanup parity | Claude | P3 non-blocking follow-up; benchmark does not report cleanup success |

## External consultation and adversarial escalation record

### `nod-1414-p1-cleanup-workspace-membership-v1`

- Council run: `20260722-122249-1e3f`.
- Substantive Gemini disposition: authorize bounded fix/pass.
- GPT failed provider resolution and Claude returned empty output; the consultation was not retried.
- Implemented: required non-empty string `workspace_id`; exact structured workspace/tab membership for every leg before any close; exact absence after close; fail-closed malformed response handling.
- Adversarial review exposed an unrelated-list envelope bug; fixed by accepting only supported direct `tabs` and `result.tabs` envelopes.
- Claude follow-up: clean for the workspace cleanup family.

### `nod-1414-p1-prompt-settlement-sequencing-v1`

- Council run: `20260722-125745-5f61`.
- Gemini disposition: authorize bounded fix/pass.
- Dogfood evidence: after prompt submission, a settled-state wait returned against the pre-prompt idle/done state and read stale output.
- Implemented: capture actual `state_change_seq` before submit; after all prompts are accepted, each concurrent wait requires a strictly newer sequence within the bounded first-transition deadline; direct newer settled state is accepted; working state uses remaining timeout; malformed/decreasing/non-advancing sequence fails as transport failure.

### `nod-1414-p1-result-transport-reliability-v1`

- Council run: `20260722-131347-67fe`.
- Gemini disposition: authorize bounded fix/pass for all three concrete transport concerns.
- Implemented:
  - strict line-start normalization for the known Codex `• ` and Claude `⏺ ` presentation prefixes while preserving exact nonce count/order/final verdict rules;
  - raw transcript bytes remain the receipt/cleanup digest identity;
  - bounded strictly-newer-sequence polling after the one proven stalled-prompt Enter recovery;
  - per-adapter serialization of Git fingerprint computation without serializing reviewer waits.
- First adversarial pass: Codex and Claude both found one deterministic documentation/install-assertion phrase mismatch.
- One-line fix aligned the skill sentence with the exact asserted phrase while preserving every recovery restriction.
- Allowed pass-after-fixes: Codex `CLEAN_FOR_PR`; Claude `CLEAN_FOR_PR`.

### `nod-1414-p1-cleanup-result-block-stability-v1`

- Initial council run `20260722-134712-05ff` completed with empty output; one narrowed unusable-output retry was used without new exploration.
- Narrowed council run `20260722-134834-d230` classified volatile whole-transcript cleanup identity as an in-scope P1 blocker and authorized one bounded fix/pass.
- Dogfood evidence: both reviewers returned clean results, but delayed cleanup preserved Codex and rejected Claude because dynamic TUI/footer material outside Claude's unchanged nonce block changed the whole-transcript digest.
- Implemented:
  - receipt schema `review-orchestration-result-v3` stores the accepted initial-or-retry `result_nonce` and SHA-256 of only the exact raw BEGIN-through-END block span;
  - cleanup re-extracts exactly one current block for that nonce and compares its raw-block digest;
  - dynamic preamble/footer content outside the block is tolerated, while missing/duplicate/reversed/changed blocks fail closed;
  - P4 malicious receipt tampering and cryptographic authentication remain out of scope.
- Adversarial pass: Codex `CLEAN_FOR_PR`; Claude `CLEAN_FOR_PR`.
- Production cleanup using the durable v3 receipt: `CLEANUP_COMPLETE`; coordinator-owned tabs `w8H:tA` and `w8H:tB` closed and exact absence confirmed.

## Implementation-stage PM review and fixes

The first implementation-stage PM review returned `PM_FIX_REQUIRED` for two current supported-path gaps:

1. `install.sh --pi-review-stack` installed review skills that invoke the shared orchestration helper but omitted `~/.agents/scripts/review_orchestration.py`.
2. Production results marked `first_action` only after settlement, so the event timestamp was not the first observed post-submit transition; the durable benchmark record also omitted the plan-locked distribution, count, host/tool, and raw-artifact-hash fields.

Implemented:

- the bounded direct and transactional Pi review-stack paths now install, snapshot, restore, parity-check, mode-check, and smoke-check the exact orchestration runtime while preserving foreign sibling scripts and parent metadata;
- the production Herdr adapter now captures the monotonic timestamp immediately when it observes the first strictly newer post-submit `state_change_seq`, and the recorder uses that timestamp rather than settlement time;
- deterministic tests cover delayed working-to-settled separation, direct-settled observation, installer parity, tampering/mode failures, foreign sibling preservation, runtime symlink safety, and exact rollback;
- the benchmark evidence above now records medians, p75/p90, accepted/applicable counts, coverage, host/tool versions, reviewer profiles, and raw JSON hashes;
- the Enter-recovery path now retains the first transition timestamp observed by `prompt_accepted()` and consumes it exactly once during the subsequent wait, with per-submit reset and a deterministic clock-advance regression test.

Targeted PM rereview first exposed the residual Enter-recovery timestamp loss. The permitted final targeted rereview verified per-submit reset, exact observation-time retention, one-time consumption, retry safety, normal-path compatibility, and the clock-advance regression test, then returned `PM_PASS_IMPLEMENTATION`.

## Final production-path review receipt

Durable receipt: `thoughts/validation/pre-pr-reviews/2026-07-22-nod-1414-p1-final-receipt.json`

- Schema: `review-orchestration-result-v3`
- Candidate fingerprint: `374b95037149dc0324184f9f109a784ac08a1dd3b7e07bb7a0d26443045ca98e`
- `all_prompts_submitted_before_first_wait`: `true`
- Aggregate status: `CLEAN_FOR_PR`
- Codex verdict: `CLEAN_FOR_PR`
- Claude verdict: `CLEAN_FOR_PR`
- Codex elapsed: `62.234s`
- Claude elapsed: `310.274s`
- Candidate wall time: `310.418s`
- Retry used: no
- Durable cleanup artifact: `thoughts/validation/pre-pr-reviews/2026-07-22-nod-1414-p1-final-cleanup.json`
- Cleanup status: `CLEANUP_COMPLETE`

## Full pre-PR implementation review

Durable review artifact: `thoughts/validation/pre-pr-reviews/2026-07-22-nod-1414-parallel-review.md`

The first full split review found six in-scope or prerequisite defects across runtime ordering, cleanup tab binding, timing/benchmark truthfulness, Codex high-risk routing, bootstrap review-family identity, and advisory-only authority. All six were fixed. Three unrelated or later hardening suggestions were rejected or deferred with concrete scope reasons.

The targeted rereview made the runtime/policy fixes clean in Codex and clean in Claude after one manually proven Enter. That dogfood exposed a new supported transport family: `herdr agent prompt` could return success while leaving the exact rendered prompt visibly unsubmitted and the sequence unchanged. The permitted third review cycle fixed that family with one exact-proof Enter recovery inside the per-leg wait, no resubmission, no second Enter, no sibling/budget reset, remaining-time enforcement, and precise transport failure on any mismatch or failed recovery.

Final production helper receipt: `thoughts/validation/pre-pr-reviews/2026-07-22-nod-1414-parallel-review-final-receipt.json`

- Candidate fingerprint: `ea58c36269d29222f5d1d36ee472e0989e16749d5018de65b57d4ec729b395eb`
- `all_prompts_submitted_before_first_wait`: `true`
- Codex: `CLEAN_FOR_PR` in `49.675s`
- Claude Code: `CLEAN_FOR_PR` in `221.263s`
- Candidate wall: `221.496s`
- Narrowed unusable-output retry: not used
- Cleanup: `CLEANUP_COMPLETE`; exact coordinator-owned tabs `w8H:tD` and `w8H:tC` closed and confirmed absent
- Durable cleanup: `thoughts/validation/pre-pr-reviews/2026-07-22-nod-1414-parallel-review-final-cleanup.json`

## Post-rebase external consultation and review

The branch rebased onto `origin/main` `fbbd817`; conflicts were limited to `AGENTS.md` and `skills/run-plan/SKILL.md`. The resolution preserved current-main complete PR-reviewable-slice and non-blocking post-merge deployment requirements together with the branch's no-PR-prerequisite consultation policy.

The ordinary review budget was already consumed, so stable consultation identifier `nod-1414-p1-post-rebase-policy-conflict-v1` used Gemini council run `20260722-160036-04cb`. Gemini authorized one bounded Codex+Claude pass limited to those conflict resolutions and direct base-integration regressions, plus the existing one pass after fixes.

The authorized pass returned Claude `CLEAN_FOR_PR` and one Codex P2 blocker: ordinary-budget and monitoring guards still made the consultation-authorized exception unreachable. The bounded fix aligned the primary and Hermes run-plan paths so an exhausted stable family must use its one consultation; only explicit authorization permits the one bounded exception plus one pass after fixes; no renamed, unconditional, PR-created, or review-until-clean route exists. Exact source/install assertions cover the distinction.

The allowed pass after fixes returned Codex `CLEAN_FOR_PR` and Claude Code `CLEAN_FOR_PR`.

- Durable receipt: `thoughts/validation/pre-pr-reviews/2026-07-22-nod-1414-post-rebase-final-receipt.json`
- Fingerprint: `e788b5e6154b1b52267214f3c08b42a03db7f01851fecf04823fef908420fe73`
- Codex: `CLEAN_FOR_PR` in `48.954s`
- Claude Code: `CLEAN_FOR_PR` in `83.854s`
- Candidate wall: `84.023s`
- Retry used: no
- Durable cleanup: `thoughts/validation/pre-pr-reviews/2026-07-22-nod-1414-post-rebase-final-cleanup.json`
- Cleanup: `CLEANUP_COMPLETE`; exact tabs `w8H:tE` and `w8H:tF` closed

## Final P1 scoped-review status

`OPEN_PR_READY`

The complete P1 slice and requested pre-/post-PR external-escalation policy correction passed PM review, the bounded Codex plus Claude pre-PR gate, current-base verification, and the consultation-authorized post-rebase review. Manual receipt authentication remains explicitly assigned to P4. Commit amendment, push, PR creation, and current-PR-snapshot inspection remain the next lifecycle steps.
