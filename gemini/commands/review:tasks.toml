description = "Deprecated (use change plan review)"
prompt = """

This repository no longer uses task-list review commands.

Use:
- /review:change <plan path | plan slug>
- /review:change-integrate <plan path | plan slug>

"""
