interface:
  display_name: "Run Plan"
  short_description: "Execute plans without scope creep"
  default_prompt: "Use $run-plan to execute an explicit plan path through durable goal-backed implementation, scoped reviews, Codex plus applicable Claude Code pre-PR review, fixes through full P1/P2/P3 consensus, mandatory PR creation, and post-PR monitoring until the completion criteria are met."
